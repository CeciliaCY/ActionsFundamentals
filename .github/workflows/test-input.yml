name: GitHub Actions Input Test

on:
  workflow_dispatch:
    inputs:
        BASE_URL:
          description: 'base url'
          required: true

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "${{BASE_URL}}""
# if [[ "${{BASE_URL}}" == "abc"]]; then 
#            echo "abc"; 
#          fi
      